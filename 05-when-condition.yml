- name: Demo on when condition
  hosts: localhost
  vars:
    URL: google.org
  tasks:
    - name: Condition on Strings
      when: URL == "google.com"
      debug:
        msg: Hellooo

    - name: Condition on Strings
      when: URL != "google.com"
      debug:
        msg: Hellooo